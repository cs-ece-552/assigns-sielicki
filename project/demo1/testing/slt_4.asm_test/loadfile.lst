          // SLT
          // if(Rs < Rt) then Rd <- 1 else Rd <-0
          
          // Load Rs w/ smaller # than Rt
0000 c10a lbi  r1, 0x0a
0002 910a slbi r1, 0x0a
          // Load Rt w/ all ones
0004 c2ff lbi  r2, 0xff
0006 92ff slbi r2, 0xff
          // SLT with (Rs < Rt): should return 1
0008 e94c SLT r3, r1, r2
000a 0000 halt
